def select_pole_rules(self, is_stator):
    """selects step to add rules for pole

    Parameters
    ----------
    self : ConvertMC
        A ConvertMC object
    is_stator : bool
        True slot is in stator, False slot is in rotor

    """
    self.get_logger().info("pole, not implemented")
