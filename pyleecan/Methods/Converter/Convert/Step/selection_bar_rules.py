def selection_bar_rules(self, is_stator):
    """selection step to add bar rules

    Parameters
    ----------
    self : ConvertMC
        A ConvertMC object
    is_stator : bool
        True slot is in stator, False slot is in rotor

    """
    print("bar, not yet defined")
