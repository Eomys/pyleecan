def select_conductor_rules(self, is_stator):
    """selection step to add rules for conductor

    Parameters
    ----------
    self : ConvertMC
        A ConvertMC object
    is_stator : bool
        True slot is in stator, False slot is in rotor

    """
    self.get_logger().info("conductor, not implemented")
