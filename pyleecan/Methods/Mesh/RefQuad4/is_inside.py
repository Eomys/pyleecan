# -*- coding: utf-8 -*-
from numpy import dot


def is_inside(self, vertice, point, normal_t=None):
    """Check if a point is inside the element defined by the vertices.

    Parameters
    ----------
    self : RefTriangle3
        an RefTriangle3 object
    vertice : ndarray
        vertices of the element
    point : ndarray
        coordinates of the checked point
    normal_t : ndarray
        (optional) element normal vector

    Returns
    -------
    is_inside: bool
        true if the point is inside the element

    """
    # TODO
    return None
